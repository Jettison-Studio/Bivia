# Archived reviews

These files preserve previously reviewed drafts verbatim. Any earlier assertion that a static category validation error blocks the connected editor is superseded: Static helper mismatch; connected save path explicitly accepts backend catalog categories; runtime save verification pending. Standalone validate() errors do not prove a connected-save blocker. The connected save filter was inspected in App.tsx.
